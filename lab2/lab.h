#include <string>
#include <iostream>
#include <fstream>
#include <vector>
#include "sll.h"

bool loadList(std::string, SLL*&);
bool loadListInOrder(std::string, SLL*&);
bool loadListDirectly(std::string, SLL*&);
